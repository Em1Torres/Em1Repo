- Idea of Divide and conquer: that you have a problem and start spliting the problem in sub-problems, and hence repeating this process recursivly.
- Example merge sort: 

    input -> list arr[x]   -- not in order
    output -> list arr[x] -- order
    def merge(arr[x]):
        if(arr[x].size > 1):
        left=